
//export const getProducts = (id) => {
  //  let error = false;
    //return new Promise((resolve, reject) => {
  //      setTimeout(() => {
  //          if (error) {
  //              reject('Hubo un problema, intente nuevamente más tarde.');
  //          } else if (id){
  //              const producto = productos.find((item) => item.id === id); // Filtrar por ID
 //               if (productos) {
 //                   resolve(productos);
 //               } else {
  //                  reject("Producto no encontrado.");
  //              }
  //          } else {
  //              resolve(productos);
  //          }
   //     }, 2000);
 //   });
//};
